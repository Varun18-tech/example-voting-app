{
  "name": "voting-app-tests",
  "version": "1.0.0",
  "description": "Health check tests for the voting app",
  "scripts": {
    "test": "mocha test.js"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "mocha": "^9.0.0",
    "assert": "^2.0.0"
  }
}
